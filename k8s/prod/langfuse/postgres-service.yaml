apiVersion: v1
kind: Service
metadata:
  name: langfuse-postgres
  namespace: chatvote-prod
  labels:
    app: langfuse-postgres
spec:
  type: ClusterIP
  selector:
    app: langfuse-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
