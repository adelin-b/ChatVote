apiVersion: v1
kind: Service
metadata:
  name: langfuse-redis
  namespace: chatvote-prod
  labels:
    app: langfuse-redis
spec:
  type: ClusterIP
  selector:
    app: langfuse-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
