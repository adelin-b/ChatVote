apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: chatvote-prod
  labels:
    app: langfuse
spec:
  type: LoadBalancer
  selector:
    app: langfuse
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
