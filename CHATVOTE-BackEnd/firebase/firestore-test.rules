rules_version = '2';

// Permissive rules for E2E testing with Firebase emulators.
// DO NOT use in production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
